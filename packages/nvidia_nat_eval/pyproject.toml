# SPDX-FileCopyrightText: Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES.
# All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=64", "setuptools-scm>=8", "setuptools_dynamic_dependencies>=1.0.0"]


[tool.setuptools.packages.find]
where = ["src"]
include = ["nat.*"]


[tool.setuptools_scm]
git_describe_command = "git describe --long --first-parent"
root = "../.."


[project]
name = "nvidia-nat-eval"
dynamic = ["version", "dependencies", "optional-dependencies"]
requires-python = ">=3.14"
description = "Subpackage for evaluation in NVIDIA NeMo Agent Toolkit"
readme = "src/nat/meta/pypi.md"
keywords = ["ai", "rag", "agents", "evaluation"]
license = { text = "Apache-2.0" }
authors = [{ name = "NVIDIA Corporation" }]
maintainers = [{ name = "NVIDIA Corporation" }]
classifiers = [
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.14",
]

[project.urls]
documentation = "https://docs.nvidia.com/nemo/agent-toolkit/latest/"
source = "https://github.com/NVIDIA/NeMo-Agent-Toolkit"

[tool.setuptools_dynamic_dependencies]
dependencies = [
  # Keep package version constraints as open as possible to avoid conflicts with other packages. Always define a minimum
  # version when adding a new package. If unsure, default to using `~=` instead of `==`. Does not apply to nvidia-nat packages.
  # Keep sorted!!!
  #
  # Keep the base nvidia-nat-eval dependencies minimal and ATIF-only. Dependencies required for running evaluation via
  # the `nat eval` CLI must be added to the `full` optional dependency group.
  "nvidia-nat-atif == {version}",
]

[tool.setuptools_dynamic_dependencies.optional-dependencies]
full = [
  "boto3~=1.35",
  "datasets~=4.4",
  # Enables the `nat eval` CLI workflow runtime. Keep in `full` so base eval stays ATIF-only.
  "nvidia-nat-core == {version}",
  "openpyxl~=3.1",
  # Used by `nat eval` progress reporting through the CLI and evaluation runtime.
  "tqdm~=4.67"
]
test = [
  "boto3~=1.35",
  "datasets~=4.4",
  "nvidia-nat-core[async_endpoints] == {version}",
  "nvidia-nat-test == {version}",
  "openpyxl~=3.1",
  "tqdm~=4.67"
]

[tool.uv]
build-constraint-dependencies = ["setuptools>=64", "setuptools-scm>=8", "setuptools_dynamic_dependencies>=1.0.0"]
managed = true
config-settings = { editable_mode = "compat" }

[tool.uv.sources]
nvidia-nat-atif = { path = "../nvidia_nat_atif", editable = true }
nvidia-nat-core = { path = "../nvidia_nat_core", editable = true }
nvidia-nat-test = { path = "../nvidia_nat_test", editable = true }

[project.entry-points.'nat.components']
nat_dataset_loaders = "nat.plugins.eval.dataset_loader.register"
nat_evaluators = "nat.plugins.eval.register"

[project.entry-points.'nat.cli']
eval = "nat.plugins.eval.cli.commands:eval_command"
